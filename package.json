{
  "name": "fuck-ai",
  "private": true,
  "workspaces": ["worker", "skill/cli"],
  "scripts": {
    "dev": "cd worker && npm run dev",
    "deploy:worker": "cd worker && npx wrangler deploy",
    "deploy:web": "cd web && npm run build && cd .. && npx wrangler pages deploy web/out/ --project-name fuck-ai"
  }
}
